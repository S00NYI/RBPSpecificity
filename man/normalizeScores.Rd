% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utility.R
\name{normalizeScores}
\alias{normalizeScores}
\title{Normalize a Vector of Scores}
\usage{
normalizeScores(scores, method = "min_max", pseudocount = 1, ...)
}
\arguments{
\item{scores}{A numeric vector.}

\item{method}{Character string (case-insensitive), the normalization method to apply.
Supported methods: "min_max" (default), "z_score", "log2", "none".}

\item{pseudocount}{Numeric, pseudocount added before log2 transformation
(relevant only if `method = "log2"`). Default: 1.}

\item{...}{Additional arguments passed to specific normalization methods.
For "min_max", these are `a` and `b` for the target range (defaults to 0 and 1
respectively if not provided, via `minmaxNorm` defaults).}
}
\value{
A numeric vector of normalized scores.
}
\description{
Applies a specified normalization method to a numeric vector of scores.
}
\examples{
\dontrun{
  test_scores <- c(10, 20, 30, 40, 50, NA)
  normalizeScores(test_scores, method = "min_max")
  normalizeScores(test_scores, method = "min_max", a = 0, b = 10)
  normalizeScores(test_scores, method = "z_score")
  normalizeScores(c(0, 1, 3, 7, 15, NA), method = "log2") # Uses pseudocount = 1
  normalizeScores(c(0, 1, 3, 7, 15), method = "log2", pseudocount = 0.1)
  normalizeScores(test_scores, method = "none") # Returns original scores
}
}
\keyword{#}
\keyword{Or}
\keyword{be}
\keyword{could}
\keyword{exported}
\keyword{generally}
\keyword{if}
\keyword{internal}
\keyword{useful}
