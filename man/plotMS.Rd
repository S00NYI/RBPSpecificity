% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotter.R
\name{plotMS}
\alias{plotMS}
\title{Plot Mutational Sensitivity (MS) Matrix}
\usage{
plotMS(motif_enrichment, motif = NULL, ...)
}
\arguments{
\item{motif_enrichment}{A data frame containing motif enrichment scores
(output from `motifEnrichment()`). Requires 'MOTIF' and 'Score' columns.}

\item{motif}{Character string, the reference motif for which MS was calculated.
If NULL or "top", the top-scoring motif is used (default: NULL).}

\item{...}{Additional arguments passed to ggplot theme layers or geoms.}
}
\value{
A ggplot object visualizing the MS matrix (e.g., using points sized
or colored by sensitivity).
}
\description{
Visualizes the mutational sensitivity scores for a motif,
typically showing sensitivity to SNVs at each position.
}
\examples{
\dontrun{
# Assuming enrichment_results is output from motifEnrichment()
plotMS(enrichment_results, motif = "UGUGU")
}
}
