% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotter.R
\name{plotVS}
\alias{plotVS}
\title{Plot Variation Sensitivity (VS) Matrix}
\usage{
plotVS(motif_enrichment, motif = NULL, ...)
}
\arguments{
\item{motif_enrichment}{A data frame containing motif enrichment scores
(output from `motifEnrichment()`). Requires 'MOTIF' and 'Score' columns.}

\item{motif}{Character string, the reference motif for which VS was calculated.
If NULL or "top", the top-scoring motif is used (default: NULL).}

\item{...}{Additional arguments passed to ggplot theme layers or geoms.}
}
\value{
A ggplot object visualizing the VS matrix (e.g., using points sized
or colored by sensitivity).
}
\description{
Visualizes the variation sensitivity scores for a motif,
typically showing sensitivity to SNVs at each position.
}
\examples{
# Mock data with variants
motifs <- c("AAAA", "CAAA", "GAAA", "TAAA")
scores <- c(10,      5,      4,      3)
df <- data.frame(MOTIF = motifs, Score = scores)

# Plot VS for "AAAA"
plotVS(df, motif = "AAAA")
}

